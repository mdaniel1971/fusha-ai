# Anthropic - for Claude (the AI teacher)
# Get your key at: https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-xxxxx

# OpenAI - for Whisper (speech-to-text) and TTS (text-to-speech)
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-xxxxx

# Supabase - for database
# Get these from: Supabase Dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyxxxxx
# Service role key - bypasses RLS, use ONLY on server-side
# Get from: Supabase Dashboard → Settings → API → service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=eyxxxxx

# Cron job security (for weekly quota reset)
# Generate a random secret: openssl rand -hex 32
CRON_SECRET=your_random_secret_here

# Note: NEXT_PUBLIC_ prefix means the variable is available in browser code
# API keys without this prefix are server-only (more secure)
